@page
@model _3312_CIDM_FINAL_PROJECT.Pages.Plays.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Play</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Play.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Play.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Play.Description)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Play.Description)
        </dd>
        @foreach (var t in Model.Play.TeamID){
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Play.PlayID)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Play.PlayID)
        </dd>  
        }
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Play?.PlayID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
